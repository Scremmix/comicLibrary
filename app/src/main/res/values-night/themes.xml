<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ComicLibrary" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <item name="colorPrimary">@color/tealDark</item>
        <item name="colorPrimaryDark">@color/tealDark</item>
        <item name="colorAccent">@color/orangeLight</item>

        <item name="plainTextColor">@color/white</item>
        <item name="buttonTextColor">@color/white</item>
        <item name="availableColor">@color/lightGreen</item>
        <item name="notAvailableColor">@color/lightAmber</item>
        <item name="bookedColor">@color/lightRed</item>
    </style>
</resources>